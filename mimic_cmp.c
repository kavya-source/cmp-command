 #include<stdio.h>
 #include<stdlib.h>
 #include<string.h>
 #include"mimic_cmp_server.h"
 #define MAX_SIZE 200

//THE INTRODUCTION PAGE FOR cmp COMMAND
int main(){
 char c;
 printf("\n\t\t Mimicing the cmp command of LINUX\n");
 printf("\n\n cmp command in LINUX is used to compare the files, if they are identical or not\n");
 printf("\nThe options compatible with cmp command are as follows;\n");
 printf("-b option : To display the differing bytes.\n");
 printf("-i option : To skip the number of bytes seperately from the files and then compare.\n");
 printf("-s option : To supress the output where 0 if identical and 1 if non-identical.\n");
 printf("-n option : To limit till where you want to compare the files for example till 25 or 50 bytes.\n\n\n");
 MainMenu();
 
}



































	
 

 

